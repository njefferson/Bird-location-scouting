// Placeholder — regenerated by scripts/gen-water-shapes.mjs on a GitHub runner
// (.github/workflows/gen-basemap.yml; the sandbox can't reach OpenStreetMap).
// Real shorelines for the curated reservoirs, © OpenStreetMap contributors
// (ODbL). Empty until the workflow has run; the app renders nothing extra.
export const WATER_SHAPES = [];
